let a = 34;  // a starts at 34
let result = a++  // result grabs 34 FIRST, then a levels up to 35

console.log(result); // 34 — result took the photo BEFORE the level up!
console.log(a);     // 35 — a already leveled up after the assignment

