test ('has title', () => {
    console.log("Hi");
    
});

//test() is just like placeOrder() from before — it takes a name and a callback
// function, and the callback is the actual code that runs when the test executes! 